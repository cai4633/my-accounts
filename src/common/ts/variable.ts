const theme = {
  backgroundColor: "#ffda47",
  iconBackground: "#f1f1f1",
  dateBackground: "#e5e5e5",
  dateColor: "#777",
  borderColor: "#ddd",
}
const mixin = {
  noWrap: `
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;`,
}

export { theme, mixin }
